<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AUGURI Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --sidebar-width: 250px;
            --sidebar-collapsed-width: 60px;
            --transition-speed: 0.3s;
            --color-primary: #7D5531;
            --color-secondary: #7E552F;
            --color-accent: #B59979;
            --color-light: #F1DDB8;
        }

        body {
            font-family: 'Arial', sans-serif;
            overflow-x: hidden;
            background-color: var(--color-light);
        }

        #sidebar {
            position: fixed;
            top: 0;
            left: 0;
            height: 100vh;
            width: var(--sidebar-collapsed-width);
            background-color: var(--color-primary);
            transition: width var(--transition-speed) ease;
            overflow: hidden;
            z-index: 1000;
        }

        #sidebar:hover {
            width: var(--sidebar-width);
        }

        #sidebar .nav-link {
            white-space: nowrap;
            overflow: hidden;
            color: var(--color-light);
        }

        #sidebar .nav-link i {
            width: var(--sidebar-collapsed-width);
            text-align: center;
        }

        #sidebar .nav-link span {
            opacity: 0;
            transition: opacity var(--transition-speed) ease;
        }

        #sidebar:hover .nav-link span {
            opacity: 1;
        }

        #sidebar .nav-link.active {
            background-color: var(--color-secondary);
            color: var(--color-light);
        }

        #content {
            margin-left: var(--sidebar-collapsed-width);
            transition: margin-left var(--transition-speed) ease;
            background-color: var(--color-light);
        }

        #sidebar:hover + #content {
            margin-left: var(--sidebar-width);
        }

        .card {
            transition: transform 0.2s ease;
            background-color: white;
        }

        .card:hover {
            transform: translateY(-5px);
        }

        .brand-container {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
            background-color: var(--color-secondary);
        }

        .brand-name {
            color: var(--color-light);
            font-size: 1.2rem;
            font-weight: bold;
            margin-left: 0.5rem;
            white-space: nowrap;
            overflow: hidden;
        }

        #sidebar:not(:hover) .brand-name {
            display: none;
        }

        .bg-primary {
            background-color: var(--color-primary) !important;
        }

        .bg-secondary {
            background-color: var(--color-secondary) !important;
        }

        .bg-accent {
            background-color: var(--color-accent) !important;
        }

        .text-primary {
            color: var(--color-primary) !important;
        }
    </style>
</head>
<body>
    <div class="d-flex">
        <nav id="sidebar">
            <div class="brand-container">
                <i class="fas fa-mug-hot fa-3x" style="color: var(--color-light);"></i>
                <span class="brand-name">AUGURI</span>
            </div>
            <ul class="nav flex-column mt-4">
                <li class="nav-item">
                    <a class="nav-link active" href="#"><i class="fas fa-tachometer-alt fa-2x"></i> <span>Dashboard</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#"><i class="fas fa-shopping-cart fa-2x"></i> <span>Orders</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/products"><i class="fas fa-ice-cream fa-2x"></i> <span>Products</span></a>
                </li>
            </ul>
        </nav>

        <main id="content" class="flex-grow-1 p-4">
            <h1 class="mb-4 text-primary">AUGURI Dashboard</h1>
            
            <div class="row mb-4">
                <div class="col-md-3">
                    <div class="card bg-primary text-white">
                        <div class="card-body">
                            <h5 class="card-title">Total Sales</h5>
                            <p class="card-text display-6">$12,345</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="card bg-secondary text-white">
                        <div class="card-body">
                            <h5 class="card-title">Orders</h5>
                            <p class="card-text display-6">256</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="card bg-accent text-white">
                        <div class="card-body">
                            <h5 class="card-title">Customers</h5>
                            <p class="card-text display-6">1,234</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="card" style="background-color: var(--color-light); color: var(--color-primary);">
                        <div class="card-body">
                            <h5 class="card-title">Products</h5>
                            <p class="card-text display-6"><%= productCount %></p>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-md-8">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title text-primary">Sales Overview</h5>
                            <canvas id="salesChart"></canvas>
                        </div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title text-primary">Top Selling Flavors</h5>
                            <ul class="list-group list-group-flush">
                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                    Vanilla
                                    <span class="badge bg-primary rounded-pill">423</span>
                                </li>
                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                    Chocolate
                                    <span class="badge bg-primary rounded-pill">352</span>
                                </li>
                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                    Strawberry
                                    <span class="badge bg-primary rounded-pill">278</span>
                                </li>
                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                    Mint Chip
                                    <span class="badge bg-primary rounded-pill">201</span>
                                </li>
                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                    Cookie Dough
                                    <span class="badge bg-primary rounded-pill">187</span>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        // Sales Chart
        const ctx = document.getElementById('salesChart').getContext('2d');
        new Chart(ctx, {
            type: 'line',
            data: {
                labels: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun'],
                datasets: [{
                    label: 'Sales ($)',
                    data: [1200, 1900, 3000, 5000, 4000, 6000],
                    borderColor: '#7D5531',
                    backgroundColor: 'rgba(125, 85, 49, 0.1)',
                    tension: 0.1
                }]
            },
            options: {
                responsive: true,
                scales: {
                    y: {
                        beginAtZero: true
                    }
                }
            }
        });
    </script>
</body>
</html>